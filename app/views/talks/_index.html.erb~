<table class="listtable">
<tr id='list-filter' class="filter" >

<% form_tag :controller => 'talks', :action => 'list' do %>
	<th>Jméno <%= text_field_tag  "jmeno", {}, :size => 15 %>
	
	</th>
	<th> 
	<%= calendar_date_select_tag 'p_date_and_time', nil, :action => 'index', :clear_button => false, :time => false, :popup => :force, :size => 6, 
					:after_close =>  remote_function(:url => {:action => :ajax_respond_date,} , :with => 'Form.serialize(this.form)', :update => 'my_list' )
				
	%>  <br/>
	<%= calendar_date_select_tag 'p_date_and_time2', nil, :action => 'index', :clear_button => false, :time => false, :popup => :force, :size => 6, 
					:after_close =>  remote_function(:url => {:action => :ajax_respond_date,} , :with => 'Form.serialize(this.form)', :update => 'my_list' )
				
	%> 
	</th>
	
	<th colspan = 6>
	  <%= submit_tag 'Nastav filtr'%>
	</th>
	
	
	
	<% end  %> 

</tr>

<tr>
    <th>Kontakt</th>
    <th>Volat kdy </th>
    <th>Konec</th>
    <th>Poradna</th>
    <th>Poradna kdy</th>
    <th>Info</th>
    <th>Obsah</th>
     <th>Výsledek</th>
  </tr>

<% ####   vypis jednotlivzch radku   %>   
<% ####   i = row counter %> 
<%i=0%>
<% @talks.each do |talk| %>
	<% @TEXT = talk.poznamka[-200..-1] if talk.poznamka != nil %>
  <% if  @highlight =~ /\Aodd/ then @highlight = 'even' else  @highlight = 'odd' end  %>
  
  <tr id='<%= @highlight%>'>
    <td >
     	<%= link_to talk.contact.last_name + "  " + talk.contact.first_name, talk ,{ :title =>"Editace hovoru"}  if talk.poznamka != nil %>
    </td>
   	<td>
   		<%=h talk.call_when_time.strftime("%d-%m-%y|%H.%M")%>
    </td>
    <td><%=h "Ano" if talk.finished %></td>
    <td><%=h "Ano" if talk.poradna %></td>
    <td><%=h talk.poradna_when %></td>
    <td><%=h "Ano" if talk.informace_zajem %></td>
   
    <td ><%= link_to ("Ano", talk ,{ :title => @TEXT})  if talk.poznamka != "" %></td>
    <td><%=h talk.result %></td>
    </tr>
<% end %>
</table>



<h1>Listing talks</h1>

<table class="listtable">
  <tr>
    <th>Kontakt</th>
    <th>Volat kdy </th>
    <th>Konec</th>
    <th>Poradna</th>
    <th>Poradna kdy</th>
    <th>Info</th>
    <th>Poznamka</th>
  </tr>

<% ####   vypis jednotlivzch radku   %>   
<% ####   i = row counter %> 
<%i=0%>
<% @talks.each do |talk| %>
  <% if  @highlight =~ /\Aodd/ then @highlight = 'even' else  @highlight = 'odd' end  %>
  
  <tr id='<%= @highlight%>'>
    <td>
     	<%= link_to talk.contact.last_name,talk  %>
    </td>
   	<td>
   		<%=h talk.call_when_time.strftime("%d-%m-%y|%H.%M") if talk.call_when_time != nil %>
    </td>
    <td><%=h "Ano" if talk.finished %></td>
    <td><%=h "Ano" if talk.poradna %></td>
    <td><%=h talk.poradna_when %></td>
    <td><%=h "Ano" if talk.informace_zajem %></td>
    <td><%=h "Ano" if talk.poznamka != "" %></td>
    </tr>
<% end %>
</table>

<br />


class Talk < ActiveRecord::Base
	belongs_to :contact
	belongs_to :script
	
	def contact_name
			contact.last_name if contact
	end
	
	def contact_name=(last_name)
		self.contact = Contact.find_or_create_by_last_name(last_name) unless last_name.blank?
	end
end
